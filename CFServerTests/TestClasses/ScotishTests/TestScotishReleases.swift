//
//  TestScotishReleases.swift
//  CFServerTests
//
//  Created by Anthony Abbott on 19/03/2025.
//


import XCTest
@testable import CFServer



final class TestScotishReleases: XCTestCase {
   
  func testSRel1() throws {
     do {
       let sut = try Release(SRelStr1)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel2() throws {
     do {
       let sut = try Release(SRelStr2)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel3() throws {
     do {
       let sut = try Release(SRelStr3)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel4() throws {
     do {
       let sut = try Release(SRelStr4)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel5() throws {
     do {
       let sut = try Release(SRelStr5)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel6() throws {
     do {
       let sut = try Release(SRelStr6)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel7() throws {
     do {
       let sut = try Release(SRelStr7)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel8() throws {
     do {
       let sut = try Release(SRelStr8)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel9() throws {
     do {
       let sut = try Release(SRelStr9)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel10() throws {
     do {
       let sut = try Release(SRelStr10)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel11() throws {
     do {
       let sut = try Release(SRelStr11)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel12() throws {
     do {
       let sut = try Release(SRelStr12)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel13() throws {
     do {
       let sut = try Release(SRelStr13)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel14() throws {
     do {
       let sut = try Release(SRelStr14)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel15() throws {
     do {
       let sut = try Release(SRelStr15)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel16() throws {
     do {
       let sut = try Release(SRelStr16)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel17() throws {
     do {
       let sut = try Release(SRelStr17)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel18() throws {
     do {
       let sut = try Release(SRelStr18)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel19() throws {
     do {
       let sut = try Release(SRelStr19)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel20() throws {
     do {
       let sut = try Release(SRelStr20)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel21() throws {
     do {
       let sut = try Release(SRelStr21)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel22() throws {
     do {
       let sut = try Release(SRelStr22)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel23() throws {
     do {
       let sut = try Release(SRelStr23)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel24() throws {
     do {
       let sut = try Release(SRelStr24)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel25() throws {
     do {
       let sut = try Release(SRelStr25)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel26() throws {
     do {
       let sut = try Release(SRelStr26)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel27() throws {
     do {
       let sut = try Release(SRelStr27)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel28() throws {
     do {
       let sut = try Release(SRelStr28)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel29() throws {
     do {
       let sut = try Release(SRelStr29)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel30() throws {
     do {
       let sut = try Release(SRelStr30)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel31() throws {
     do {
       let sut = try Release(SRelStr31)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel32() throws {
     do {
       let sut = try Release(SRelStr32)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel33() throws {
     do {
       let sut = try Release(SRelStr33)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel34() throws {
     do {
       let sut = try Release(SRelStr34)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel35() throws {
     do {
       let sut = try Release(SRelStr35)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel36() throws {
     do {
       let sut = try Release(SRelStr36)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel37() throws {
     do {
       let sut = try Release(SRelStr37)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel38() throws {
     do {
       let sut = try Release(SRelStr38)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel39() throws {
     do {
       let sut = try Release(SRelStr39)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel40() throws {
     do {
       let sut = try Release(SRelStr40)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel41() throws {
     do {
       let sut = try Release(SRelStr41)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel42() throws {
     do {
       let sut = try Release(SRelStr42)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel43() throws {
     do {
       let sut = try Release(SRelStr43)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel44() throws {
     do {
       let sut = try Release(SRelStr44)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel45() throws {
     do {
       let sut = try Release(SRelStr45)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel46() throws {
     do {
       let sut = try Release(SRelStr46)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel47() throws {
     do {
       let sut = try Release(SRelStr47)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel48() throws {
     do {
       let sut = try Release(SRelStr48)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel49() throws {
     do {
       let sut = try Release(SRelStr49)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel50() throws {
     do {
       let sut = try Release(SRelStr50)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel51() throws {
     do {
       let sut = try Release(SRelStr51)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel52() throws {
     do {
       let sut = try Release(SRelStr52)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel53() throws {
     do {
       let sut = try Release(SRelStr53)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel54() throws {
     do {
       let sut = try Release(SRelStr54)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel55() throws {
     do {
       let sut = try Release(SRelStr55)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel56() throws {
     do {
       let sut = try Release(SRelStr56)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel57() throws {
     do {
       let sut = try Release(SRelStr57)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel58() throws {
     do {
       let sut = try Release(SRelStr58)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel59() throws {
     do {
       let sut = try Release(SRelStr59)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel60() throws {
     do {
       let sut = try Release(SRelStr60)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel61() throws {
     do {
       let sut = try Release(SRelStr61)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel62() throws {
     do {
       let sut = try Release(SRelStr62)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel63() throws {
     do {
       let sut = try Release(SRelStr63)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel64() throws {
     do {
       let sut = try Release(SRelStr64)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel65() throws {
     do {
       let sut = try Release(SRelStr65)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel66() throws {
     do {
       let sut = try Release(SRelStr66)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel67() throws {
     do {
       let sut = try Release(SRelStr67)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel68() throws {
     do {
       let sut = try Release(SRelStr68)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel69() throws {
     do {
       let sut = try Release(SRelStr69)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel70() throws {
     do {
       let sut = try Release(SRelStr70)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel71() throws {
     do {
       let sut = try Release(SRelStr71)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel72() throws {
     do {
       let sut = try Release(SRelStr72)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel73() throws {
     do {
       let sut = try Release(SRelStr73)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel74() throws {
     do {
       let sut = try Release(SRelStr74)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel75() throws {
     do {
       let sut = try Release(SRelStr75)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel76() throws {
     do {
       let sut = try Release(SRelStr76)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel77() throws {
     do {
       let sut = try Release(SRelStr77)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel78() throws {
     do {
       let sut = try Release(SRelStr78)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel79() throws {
     do {
       let sut = try Release(SRelStr79)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel80() throws {
     do {
       let sut = try Release(SRelStr80)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel81() throws {
     do {
       let sut = try Release(SRelStr81)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel82() throws {
     do {
       let sut = try Release(SRelStr82)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel83() throws {
     do {
       let sut = try Release(SRelStr83)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel84() throws {
     do {
       let sut = try Release(SRelStr84)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel85() throws {
     do {
       let sut = try Release(SRelStr85)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel86() throws {
     do {
       let sut = try Release(SRelStr86)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel87() throws {
     do {
       let sut = try Release(SRelStr87)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel88() throws {
     do {
       let sut = try Release(SRelStr88)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel89() throws {
     do {
       let sut = try Release(SRelStr89)
     } catch {
       failWithDetailedError(error)
     }
   }


  func testSRel90() throws {
     do {
       let sut = try Release(SRelStr90)
     } catch {
       failWithDetailedError(error)
     }
   }



}
