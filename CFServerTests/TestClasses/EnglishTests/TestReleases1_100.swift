//
//  TestReleases1_100.swift
//  CFServerTests
//
//  Created by Anthony Abbott on 11/03/2025.
//
//

import XCTest
@testable import CFServer

final class TestReleases1_100: XCTestCase {
  
  
  func testRelease1() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData1)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease2() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData2)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease3() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData3)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease4() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData4)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease5() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData5)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease6() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData6)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease7() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData7)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease8() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData8)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease9() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData9)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease10() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData10)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease11() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData11)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease12() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData12)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease13() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData13)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease14() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData14)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease15() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData15)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease16() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData16)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease17() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData17)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease18() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData18)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease19() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData19)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease20() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData20)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease21() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData21)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease22() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData22)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease23() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData23)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease24() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData24)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease25() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData25)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease26() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData26)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease27() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData27)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease28() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData28)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease29() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData29)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease30() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData30)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease31() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData31)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease32() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData32)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease33() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData33)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease34() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData34)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease35() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData35)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease36() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData36)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease37() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData37)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease38() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData38)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease39() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData39)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease40() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData40)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease41() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData41)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  /// Originally had '"' appearing in the desxcription field which invalidated the JSON.
  /// I first tried to escape the embedded quotes (\"), but thisdidnt fix the problem
  func testRelease42() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData42)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease43() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData43)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease44() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData44)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease45() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData45)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease46() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData46)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease47() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData47)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease48() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData48)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease49() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData49)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease50() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData50)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease51() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData51)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease52() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData52)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease53() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData53)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease54() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData54)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease55() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData55)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease56() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData56)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease57() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData57)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease58() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData58)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease59() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData59)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease60() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData60)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease61() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData61)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease62() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData62)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease63() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData63)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease64() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData64)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease65() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData65)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease66() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData66)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease67() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData67)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease68() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData68)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease69() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData69)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease70() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData70)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease71() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData71)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease72() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData72)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease73() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData73)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease74() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData74)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease75() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData75)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease76() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData76)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease77() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData77)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease78() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData78)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease79() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData79)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease80() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData80)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease81() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData81)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease82() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData82)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease83() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData83)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease84() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData84)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease85() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData85)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease86() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData86)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease87() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData87)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease88() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData88)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease89() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData89)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease90() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData90)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease91() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData91)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease92() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData92)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease93() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData93)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease94() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData94)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease95() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData95)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease96() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData96)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease97() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData97)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease98() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData98)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease99() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData99)
    XCTAssertTrue(sut.successfulParse)
  }
  
  
  func testRelease100() throws {
    let sut = CFServer()
    sut.parse(json: CFSearchData100)
    XCTAssertTrue(sut.successfulParse)
  }
}

