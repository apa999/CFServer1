//
//  TestWelshReleases.swift
//  CFServerTests
//
//  Created by Anthony Abbott on 14/03/2025.
//

import XCTest
@testable import CFServer



final class TestWelshReleases: XCTestCase {
  
  func testWRel1() throws {
    do {
      let sut = try Release(WRel1)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel2() throws {
    do {
      let sut = try Release(WRel2)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel3() throws {
    do {
      let sut = try Release(WRel3)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel4() throws {
    do {
      let sut = try Release(WRel4)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel5() throws {
    do {
      let sut = try Release(WRel5)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel6() throws {
    do {
      let sut = try Release(WRel6)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel7() throws {
    do {
      let sut = try Release(WRel7)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel8() throws {
    do {
      let sut = try Release(WRel8)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel9() throws {
    do {
      let sut = try Release(WRel9)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel10() throws {
    do {
      let sut = try Release(WRel10)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel11() throws {
    do {
      let sut = try Release(WRel11)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel12() throws {
    do {
      let sut = try Release(WRel12)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel13() throws {
    do {
      let sut = try Release(WRel13)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel14() throws {
    do {
      let sut = try Release(WRel14)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel15() throws {
    do {
      let sut = try Release(WRel15)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel16() throws {
    do {
      let sut = try Release(WRel16)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel17() throws {
    do {
      let sut = try Release(WRel17)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel18() throws {
    do {
      let sut = try Release(WRel18)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel19() throws {
    do {
      let sut = try Release(WRel19)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel20() throws {
    do {
      let sut = try Release(WRel20)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel21() throws {
    do {
      let sut = try Release(WRel21)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel22() throws {
    do {
      let sut = try Release(WRel22)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel23() throws {
    do {
      let sut = try Release(WRel23)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel24() throws {
    do {
      let sut = try Release(WRel24)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel25() throws {
    do {
      let sut = try Release(WRel25)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel26() throws {
    do {
      let sut = try Release(WRel26)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel27() throws {
    do {
      let sut = try Release(WRel27)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel28() throws {
    do {
      let sut = try Release(WRel28)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel29() throws {
    do {
      let sut = try Release(WRel29)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel30() throws {
    do {
      let sut = try Release(WRel30)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel31() throws {
    do {
      let sut = try Release(WRel31)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel32() throws {
    do {
      let sut = try Release(WRel32)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel33() throws {
    do {
      let sut = try Release(WRel33)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel34() throws {
    do {
      let sut = try Release(WRel34)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel35() throws {
    do {
      let sut = try Release(WRel35)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel36() throws {
    do {
      let sut = try Release(WRel36)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel37() throws {
    do {
      let sut = try Release(WRel37)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel38() throws {
    do {
      let sut = try Release(WRel38)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel39() throws {
    do {
      let sut = try Release(WRel39)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel40() throws {
    do {
      let sut = try Release(WRel40)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel41() throws {
    do {
      let sut = try Release(WRel41)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel42() throws {
    do {
      let sut = try Release(WRel42)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel43() throws {
    do {
      let sut = try Release(WRel43)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel44() throws {
    do {
      let sut = try Release(WRel44)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel45() throws {
    do {
      let sut = try Release(WRel45)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel46() throws {
    do {
      let sut = try Release(WRel46)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel47() throws {
    do {
      let sut = try Release(WRel47)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel48() throws {
    do {
      let sut = try Release(WRel48)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel49() throws {
    do {
      let sut = try Release(WRel49)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel50() throws {
    do {
      let sut = try Release(WRel50)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel51() throws {
    do {
      let sut = try Release(WRel51)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel52() throws {
    do {
      let sut = try Release(WRel52)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel53() throws {
    do {
      let sut = try Release(WRel53)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel54() throws {
    do {
      let sut = try Release(WRel54)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel55() throws {
    do {
      let sut = try Release(WRel55)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel56() throws {
    do {
      let sut = try Release(WRel56)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel57() throws {
    do {
      let sut = try Release(WRel57)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel58() throws {
    do {
      let sut = try Release(WRel58)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel59() throws {
    do {
      let sut = try Release(WRel59)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel60() throws {
    do {
      let sut = try Release(WRel60)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel61() throws {
    do {
      let sut = try Release(WRel61)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel62() throws {
    do {
      let sut = try Release(WRel62)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel63() throws {
    do {
      let sut = try Release(WRel63)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel64() throws {
    do {
      let sut = try Release(WRel64)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel65() throws {
    do {
      let sut = try Release(WRel65)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel66() throws {
    do {
      let sut = try Release(WRel66)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel67() throws {
    do {
      let sut = try Release(WRel67)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel68() throws {
    do {
      let sut = try Release(WRel68)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel69() throws {
    do {
      let sut = try Release(WRel69)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel70() throws {
    do {
      let sut = try Release(WRel70)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel71() throws {
    do {
      let sut = try Release(WRel71)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel72() throws {
    do {
      let sut = try Release(WRel72)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel73() throws {
    do {
      let sut = try Release(WRel73)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel74() throws {
    do {
      let sut = try Release(WRel74)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel75() throws {
    do {
      let sut = try Release(WRel75)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel76() throws {
    do {
      let sut = try Release(WRel76)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel77() throws {
    do {
      let sut = try Release(WRel77)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel78() throws {
    do {
      let sut = try Release(WRel78)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel79() throws {
    do {
      let sut = try Release(WRel79)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel80() throws {
    do {
      let sut = try Release(WRel80)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel81() throws {
    do {
      let sut = try Release(WRel81)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel82() throws {
    do {
      let sut = try Release(WRel82)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel83() throws {
    do {
      let sut = try Release(WRel83)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel84() throws {
    do {
      let sut = try Release(WRel84)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel85() throws {
    do {
      let sut = try Release(WRel85)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel86() throws {
    do {
      let sut = try Release(WRel86)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel87() throws {
    do {
      let sut = try Release(WRel87)
    } catch {
      failWithDetailedError(error)
    }
  }
  
  func testWRel88() throws {
    do {
      let sut = try Release(WRel88)
    } catch {
      failWithDetailedError(error)
    }
  }
}
